#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stdlib.h>
#include <stdio.h>
#include <limits.h>

/**
 * print_x - typedef for struct print
 */
typedef struct print print_x;

int _printf(const char *format, ...);
int _putchar(char c);

#endif /* MAIN_H */
